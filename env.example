# ========================================
# 🔐 SEGURIDAD (LLAVES MAESTRAS)
# ========================================
# Estas claves serán validadas por Nginx antes de llegar a la App
COGNEE_API_KEY=xxx
# ========================================
# 🧠 PROVEEDORES DE IA
# ========================================
OPENAI_API_KEY=sk-proj-xxx
# Configuración estándar compartida
LLM_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_DIMENSIONS=1536

# ========================================
# 🐘 INFRAESTRUCTURA (POSTGRES & NEO4J)
# ========================================
# Nota: La creación de DBs específicas se hace en el init.sql
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=mem0graph

REDIS_HOST=redis
REDIS_PORT=6379


# ========================================
# 🔧 CONFIGURACIÓN ESPECÍFICA COGNEE
# ========================================
# Desactivamos auth nativa (Nginx se encarga)
REQUIRE_AUTHENTICATION=false
# Definimos Postgres como motor vectorial (Unificación)
VECTOR_DB_PROVIDER=PGVector
DB_PROVIDER=postgres